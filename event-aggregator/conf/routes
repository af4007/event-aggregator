# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
POST    /sendEvent                  controllers.Application.sendEvent
GET     /findEvent/:eventType		controllers.Application.findEvent(eventType: String)
GET     /dispatcher      			controllers.Application.dispatcher
